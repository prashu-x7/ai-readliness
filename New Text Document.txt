https://ai-readiness-score-frontend.vercel.app/



Because different developers use different names for the same concept. 
A Python developer writes jwt.encode().
 A Node.js developer writes 

require('jsonwebtoken')
. A FastAPI developer uses HTTPBearer. A Django developer uses oauth2.

All 12 patterns are checking the same question: "Does this project have authentication?"

If even ONE of the 12 patterns is found on any line in any file → Rule R01 = PASSED → project earns 8 points.

If NONE of the 12 patterns are found in the entire project → Rule R01 = FAILED → 0 points and it becomes a Hard Blocker.